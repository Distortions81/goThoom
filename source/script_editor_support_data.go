// Code generated by gt2/cmd/apiref; DO NOT EDIT.
package main

var embeddedGT2EditorFiles = []struct {
	path string
	data []byte
}{
	{path: "gt2/go.mod", data: []byte("module gt2\n\ngo 1.27.1\n")},
	{path: "gt2/pluginapi.go", data: []byte("// Canonical public contract for goThoom scripting API 2.\n// Runtime exports and the generated API reference are verified against this\n// file. Implementations are no-ops so editors can type-check scripts without\n// the full client.\n//\n// Permissions: the client infers requested capabilities from API references.\n// First enable requires user review in a Grant / Block all dialog before globals\n// or Init run. Commands, key bindings, sending server commands, data/events,\n// messages, movement, windows/overlays, input, notifications, storage, Repeat and\n// session events have separate grants. Denied calls do nothing and return zero\n// values or inactive handles. Print, basic configuration, Wait/WaitTicks and\n// cleanup helpers remain available after review. See the script guide for the\n// complete mapping. Decisions are per script ID across characters and persist\n// in Scripts/permissions.json. New capabilities require another review;\n// changing grants restarts the script and releases its existing resources.\npackage gt2\n\nimport \"time\"\n\n// Version and client info\nvar CLVersion int\n\n// Basic output\nfunc Print(msg string)            {}\nfunc ShowNotification(msg string) {}\n\n// Commands\ntype Subscription struct{}\n\nfunc (Subscription) Remove()      {}\nfunc (Subscription) Active() bool { return false }\n\ntype Timer struct{}\n\nfunc (Timer) Stop()        {}\nfunc (Timer) Active() bool { return false }\n\nfunc Command(name string, handler func(args string)) Subscription { return Subscription{} }\n\n// QueueCommand returns delivery feedback. Sent means a successful network write,\n// not that the server accepted or completed the action. Cancel only removes unsent work.\nfunc QueueCommand(cmd string) CommandTicket { return CommandTicket{} }\n\ntype CommandTicket struct{}\n\nfunc (CommandTicket) Status() CommandStatus { return CommandStatus{} }\nfunc (CommandTicket) Cancel() bool          { return false }\n\ntype CommandStatus struct{ State, Reason string }\n\nconst (\n\tCommandQueued    = \"queued\"\n\tCommandSent      = \"sent\"\n\tCommandCancelled = \"cancelled\"\n\tCommandRejected  = \"rejected\"\n)\n\n// HasPermission checks a permission ID from the script guide. Unknown IDs return false.\nfunc HasPermission(permission string) bool { return false }\n\nfunc Send(cmd string) {}\n\n// Hotkeys\ntype InputEvent struct {\n\tChord      string\n\tKey        string\n\tButton     string\n\tModifiers  []string\n\tCtrl       bool\n\tAlt        bool\n\tShift      bool\n\tMeta       bool\n\tScreenX    int\n\tScreenY    int\n\tWorldX     int16\n\tWorldY     int16\n\tOnMobile   bool\n\tMobile     Mobile\n\tPlayerName string\n\tSimpleName string\n}\n\nfunc (InputEvent) Consume()        {}\nfunc (InputEvent) Pass()           {}\nfunc (InputEvent) Continues() bool { return true }\n\nfunc Bind(combo string, handler func(InputEvent)) Subscription { return Subscription{} }\n\n// Toolbars\ntype ToolbarButton struct {\n\tLabel, Tooltip, Icon, Key string\n\tOnClick                   func()\n}\n\ntype ToolbarOptions struct {\n\tLabel   string\n\tButtons []ToolbarButton\n}\n\n// AddToolbar adds a row to the client toolbar. Icon paths are relative to the\n// root of a folder or ZIP script and are loaded by goThoom.\nfunc AddToolbar(options ToolbarOptions) Subscription { return Subscription{} }\n\n// Script windows are independent, movable client windows. All UI changes are\n// marshaled to the client thread. Button and close callbacks run on the script's\n// serialized event queue. Windows are removed when their script stops/reloads.\ntype WindowButton struct {\n\tID, Label, Tooltip string\n\tIcon               string // Optional bundled Material icon name, such as \"settings\".\n\tDisabled           bool\n\tWidth              int // Optional width in logical pixels for inline rows.\n\tOnClick            func()\n}\n\nconst (\n\tControlText     = \"text\"\n\tControlCheckbox = \"checkbox\"\n\tControlDropdown = \"dropdown\"\n\tControlList     = \"list\"\n\tControlColor    = \"color\"\n\tControlImage    = \"image\"\n)\n\n// WindowControl IDs share a namespace with button IDs. Lists show selectable rows.\n// Color is RGBA packed as 0xRRGGBBAA. OnChange runs for user edits only.\n// Selected is -1 for no selection.\ntype WindowControl struct {\n\tID, Label, Kind, Text, Tooltip string\n\tChecked, Disabled              bool\n\tColor                          uint32\n\tImage                          uint16 // Sprite ID for ControlImage; zero shows an empty preview.\n\tWidth                          int    // Optional width in logical pixels for inline rows.\n\tOptions                        []string\n\tOptionImages                   []uint16 // Optional sprite IDs aligned with list options.\n\tSelected                       int\n\tOnChange                       func(WindowControlEvent)\n}\ntype WindowControlEvent struct {\n\tID, Text string\n\tChecked  bool\n\tColor    uint32\n\tSelected int\n}\n\n// WindowRow arranges controls and buttons on one line. Control labels from the\n// first row form the column headings. IDs are unique across the whole window.\ntype WindowRow struct {\n\tControls []WindowControl\n\tButtons  []WindowButton\n}\n\ntype WindowOptions struct {\n\tControls    []WindowControl\n\tRows        []WindowRow // Editable rows below Controls and above Buttons, with a scrolling viewport.\n\tTitle, Text string\n\tWidth       int            // Content width in logical pixels; default 340, clamped to 220..800.\n\tButtons     []WindowButton // Up to eight buttons, arranged two per row; IDs must be unique.\n\tOnClose     func()         // Called when the user closes the window or Hide is called.\n}\n\ntype Window struct{}\n\n// CreateWindow opens a window after successful script activation. Init and\n// validation stage all UI changes; failed scripts never open a window.\nfunc CreateWindow(options WindowOptions) Window { return Window{} }\n\nfunc (Window) SetText(text string)                      {}\nfunc (Window) SetButtonEnabled(id string, enabled bool) {}\n\n// SetRows replaces the inline rows while retaining the window's position.\nfunc (Window) SetRows(rows []WindowRow)                {}\nfunc (Window) SetControlImage(id string, image uint16) {}\n\n// Programmatic changes do not fire OnChange. Invalid IDs/selections are ignored.\nfunc (Window) SetControlText(id, value string)            {}\nfunc (Window) SetControlChecked(id string, checked bool)  {}\nfunc (Window) SetControlColor(id string, color uint32)    {}\nfunc (Window) SetControlSelected(id string, selected int) {}\nfunc (Window) SetControlOptions(id string, options []string, optionImages ...[]uint16) {\n}\nfunc (Window) SetControlEnabled(id string, enabled bool) {}\nfunc (Window) Show()                                     {}\nfunc (Window) Hide()                                     {}\n\n// Remove permanently removes the window without calling OnClose.\nfunc (Window) Remove() {}\n\n// Active reports whether the window exists, including while hidden.\nfunc (Window) Active() bool { return false }\n\n// Shortcuts\nfunc AddShortcut(short, full string) {}\n\nconst (\n\tScopeGlobal    = \"global\"\n\tScopeCharacter = \"character\"\n)\n\ntype BoolOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 bool\n\tValidate                func(bool) bool\n\tOnChange                func(bool)\n}\n\n// ColorOption stores RGBA as 0xRRGGBBAA and uses the client's color picker.\ntype ColorOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 uint32\n\tValidate                func(uint32) bool\n\tOnChange                func(uint32)\n}\n\ntype IntegerOption struct {\n\tKey, Label, Help, Scope string\n\tDefault, Min, Max, Step int\n\tValidate                func(int) bool\n\tOnChange                func(int)\n}\n\ntype DecimalOption struct {\n\tKey, Label, Help, Scope string\n\tDefault, Min, Max, Step float64\n\tValidate                func(float64) bool\n\tOnChange                func(float64)\n}\n\ntype TextOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tValidate                func(string) bool\n\tOnChange                func(string)\n}\n\ntype ChoiceOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tChoices                 []string\n\tOnChange                func(string)\n}\n\ntype KeyBindingOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tOnChange                func(string)\n}\n\ntype ItemOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tOnChange                func(string)\n}\n\nfunc Bool(option BoolOption) bool               { return option.Default }\nfunc Color(option ColorOption) uint32           { return option.Default }\nfunc Integer(option IntegerOption) int          { return option.Default }\nfunc Decimal(option DecimalOption) float64      { return option.Default }\nfunc Text(option TextOption) string             { return option.Default }\nfunc Choice(option ChoiceOption) string         { return option.Default }\nfunc KeyBinding(option KeyBindingOption) string { return option.Default }\nfunc ItemSelector(option ItemOption) string     { return option.Default }\n\n// OpenSettings opens this script's native preferences, key bindings, and\n// commands page. Calls made before Init completes are ignored.\nfunc OpenSettings() {}\n\n// Storage is private to the script that calls it, but shared by every\n// character using that script. Include the current character in the key when\n// data should be character-specific.\nfunc Store(key string, value any)                               {}\nfunc LoadString(key, fallback string) string                    { return fallback }\nfunc LoadBool(key string, fallback bool) bool                   { return fallback }\nfunc LoadInteger(key string, fallback int) int                  { return fallback }\nfunc LoadDecimal(key string, fallback float64) float64          { return fallback }\nfunc LoadStrings(key string, fallback []string) []string        { return fallback }\nfunc LoadJSON(key string, target any) bool                      { return false }\nfunc DeleteStored(key string)                                   {}\nfunc MigrateStorage(version int, migrate func(fromVersion int)) {}\n\n// Storage is a character-bound handle. An unknown character produces an inactive\n// handle; obtain a new one after login. Retained handles can save during logout\n// and Terminate. Methods preserve ordinary storage fallbacks.\ntype Storage struct{}\n\nfunc CharacterStore() Storage                                      { return Storage{} }\nfunc (Storage) Active() bool                                       { return false }\nfunc (Storage) Store(key string, value any)                        {}\nfunc (Storage) LoadString(key, fallback string) string             { return fallback }\nfunc (Storage) LoadBool(key string, fallback bool) bool            { return fallback }\nfunc (Storage) LoadInteger(key string, fallback int) int           { return fallback }\nfunc (Storage) LoadDecimal(key string, fallback float64) float64   { return fallback }\nfunc (Storage) LoadStrings(key string, fallback []string) []string { return fallback }\nfunc (Storage) LoadJSON(key string, target any) bool               { return false }\nfunc (Storage) DeleteStored(key string)                            {}\n\n// Input box helpers\nfunc InputText() string        { return \"\" }\nfunc SetInputText(text string) {}\n\n// Player and world info\ntype Player struct {\n\tName         string\n\tRace         string\n\tGender       string\n\tClass        string\n\tPictID       uint16\n\tColors       []byte\n\tIsNPC        bool\n\tSharee       bool\n\tSharing      bool\n\tFriend       bool\n\tFriendLabel  int\n\tLocalLabel   int\n\tGlobalLabel  int\n\tBlocked      bool\n\tIgnored      bool\n\tDead         bool\n\tFellWhere    string\n\tFellTime     time.Time\n\tKillerName   string\n\tBard         bool\n\tSameClan     bool\n\tSeen         bool\n\tLastSeen     time.Time\n\tLastOnScreen time.Time\n\tOffline      bool\n}\n\ntype Character struct {\n\tName       string\n\tHealth     int\n\tHealthMax  int\n\tSpirit     int\n\tSpiritMax  int\n\tBalance    int\n\tBalanceMax int\n\tLocation   string\n\tEquipment  []Item\n}\n\nfunc Self() Character   { return Character{} }\nfunc Players() []Player { return nil }\n\n// PlayerChangeEvent reports observed changes, with detached before/after snapshots.\n// First observation establishes a baseline. New known players use PlayerDiscovered;\n// login/logout mean an observed Offline transition, not entering/leaving the screen.\ntype PlayerChangeEvent struct {\n\tType             string\n\tPrevious, Player Player\n}\n\nconst (\n\tPlayerDiscovered = \"discovered\"\n\tPlayerRemoved    = \"removed\"\n\tPlayerLogin      = \"login\"\n\tPlayerLogout     = \"logout\"\n\tPlayerFallen     = \"fallen\"\n\tPlayerRecovered  = \"recovered\"\n\tPlayerSharing    = \"sharing\"\n)\n\nfunc OnPlayerChange(handler func(PlayerChangeEvent)) Subscription { return Subscription{} }\n\n// Inventory\ntype Item struct {\n\tInstanceID uint64\n\tID         uint16\n\tName       string\n\tBase       string\n\tExtra      string\n\tEquipped   bool\n\tIndex      int\n\tIDIndex    int\n\tQuantity   int\n\tSlot       string\n}\n\nconst (\n\tSlotForehead  = \"forehead\"\n\tSlotNeck      = \"neck\"\n\tSlotShoulder  = \"shoulder\"\n\tSlotArms      = \"arms\"\n\tSlotGloves    = \"gloves\"\n\tSlotFinger    = \"finger\"\n\tSlotCoat      = \"coat\"\n\tSlotCloak     = \"cloak\"\n\tSlotTorso     = \"torso\"\n\tSlotWaist     = \"waist\"\n\tSlotLegs      = \"legs\"\n\tSlotFeet      = \"feet\"\n\tSlotRightHand = \"right-hand\"\n\tSlotLeftHand  = \"left-hand\"\n\tSlotBothHands = \"both-hands\"\n\tSlotHead      = \"head\"\n)\n\nfunc Inventory() []Item                      { return nil }\nfunc EquippedItems() []Item                  { return nil }\nfunc FindItemExact(name string) (Item, bool) { return Item{}, false }\nfunc FindItem(name string) (Item, bool)      { return Item{}, false }\nfunc FindItems(name string) []Item           { return nil }\nfunc SearchItems(text string) []Item         { return nil }\nfunc Equipped(slot string) (Item, bool)      { return Item{}, false }\nfunc HasItem(name string) bool               { return false }\nfunc IsEquipped(name string) bool            { return false }\n\n// Equip equips an item by name (case-insensitive).\nfunc Equip(name string) {}\n\n// Unequip unequips an item by name (case-insensitive).\nfunc Unequip(name string) {}\n\nfunc WithEquipment(name string, task func()) {}\n\n// Images and world overlay\nfunc WorldSize() (int, int)                              { return 0, 0 }\nfunc ImageSize(id uint16) (int, int)                     { return 0, 0 }\nfunc OverlayClear()                                      {}\nfunc OverlayRect(x, y, w, h int, r, g, b, a uint8)       {}\nfunc OverlayCircle(x, y, radius int, r, g, b, a uint8)    {}\nfunc OverlayText(x, y int, txt string, r, g, b, a uint8) {}\nfunc OverlayImage(id uint16, x, y int)                   {}\nfunc OverlayFollowPlayer(name string, x, y, radius int, r, g, b, a uint8, maxLife time.Duration) {}\nfunc OverlayFollowMobile(index uint8, x, y, radius int, r, g, b, a uint8, maxLife time.Duration) {}\nfunc OverlayFollowBackground(pictID uint16, x, y, radius int, r, g, b, a uint8, maxLife time.Duration) {}\n\n// SetMobileTint applies a multiplicative RGBA tint to every visible mobile\n// using id. The tint belongs to the calling script and is released when it\n// stops or reloads. More than one script tinting an ID uses the\n// lexicographically last script ID.\nfunc SetMobileTint(id uint16, r, g, b, a uint8) {}\n\n// ClearMobileTint removes this script's tint for one mobile sprite ID.\nfunc ClearMobileTint(id uint16) {}\n\n// ClearMobileTints removes every mobile tint owned by this script.\nfunc ClearMobileTints() {}\n\n// SetMobileOutline draws a sprite-shaped outline around every visible mobile\n// using id. It follows the client's interpolated mobile position and is\n// released when the calling script stops or reloads.\nfunc SetMobileOutline(id uint16, r, g, b, a uint8) {}\n\n// ClearMobileOutline removes this script's outline for one mobile sprite ID.\nfunc ClearMobileOutline(id uint16) {}\n\n// ClearMobileOutlines removes every mobile outline owned by this script.\nfunc ClearMobileOutlines() {}\n\n// SetNamedMobileTint applies a tint to an exact mobile name, ignoring case and\n// leading/trailing spaces. It matches independently of sprite ID and takes\n// precedence over sprite-ID tints. Conflicting name tints use the\n// lexicographically last script ID. Empty names are ignored. Stop/reload and\n// ClearMobileTints release these tints along with sprite-ID tints.\nfunc SetNamedMobileTint(name string, r, g, b, a uint8) {}\n\n// ClearNamedMobileTint removes this script's tint for one mobile name.\nfunc ClearNamedMobileTint(name string) {}\n\n// SetNamedMobileOutline draws an outline for an exact mobile name, using the\n// same matching and precedence rules as SetNamedMobileTint. Stop/reload and\n// ClearMobileOutlines release both named and sprite-ID outlines.\nfunc SetNamedMobileOutline(name string, r, g, b, a uint8) {}\n\n// ClearNamedMobileOutline removes this script's outline for one mobile name.\nfunc ClearNamedMobileOutline(name string) {}\n\n// FlashMobile replaces one visible mobile's colors for duration, preserving its\n// sprite transparency. Alpha is the flash strength: 255 is solid color, and\n// lower values mix with the normal artwork. The flash belongs to\n// the calling script and is released when it stops or reloads. More than one\n// script flashing an index uses the lexicographically last script ID.\nfunc FlashMobile(index uint8, r, g, b, a uint8, duration time.Duration) {}\n\n// Mobile coordinates are unscaled, relative to the center of the playfield.\n// Index is a temporary descriptor slot; use Name to track a player across scenes.\ntype Mobile struct {\n\tIndex  uint8\n\tName   string\n\tH, V   int16\n\tPictID uint16\n\tColors uint8\n\tPlayer bool\n\tState  uint8 // Raw protocol animation state.\n\tPlane  int   // Draw order, not a collision layer.\n\tSize   int   // Sprite frame size, not a collision radius.\n\tSelf   bool\n\tDead   bool\n\tStale  bool // Retained for edge rendering; absent from the current server frame.\n}\n\ntype Click struct {\n\tX, Y     int16\n\tOnMobile bool\n\tOnPlayer bool\n\tMobile   Mobile\n\tButton   string\n\tCtrl     bool\n\tAlt      bool\n\tShift    bool\n\tMeta     bool\n}\n\nfunc LastClick() Click { return Click{} }\nfunc Hover() Click     { return Click{} }\n\nfunc SelectedPlayer() (Player, bool) { return Player{}, false }\nfunc SelectedItem() (Item, bool)     { return Item{}, false }\n\n// Picture describes scenery artwork. H,V is its top-left corner in centered\n// world coordinates. Dimensions describe one animation frame. No collision\n// geometry is sent by the server; planes and artwork are only navigation hints.\ntype Picture struct {\n\tPictID             uint16\n\tH, V               int16\n\tWidth, Height      int\n\tPlane              int\n\tMoving, Background bool\n\tReused             bool // Reused from a preceding frame, including retained edge artwork.\n\tShadow             bool\n}\n\ntype World struct {\n\tWidth                      int\n\tHeight                     int\n\tLocation                   string\n\tGeneration                 uint64\n\tMobiles                    []Mobile // Includes fallen and retained edge mobiles; check Dead/Stale.\n\tPictures                   []Picture\n\tSelf                       Mobile\n\tHasSelf                    bool\n\tFrame                      int // Server logical frame; may reset on reconnect or movie seek.\n\tReceivedAt                 time.Time\n\tCameraShiftX, CameraShiftY int   // Estimated scenery shift since the preceding frame.\n\tLighting                   uint8 // Raw protocol lighting flags.\n}\n\nfunc CurrentWorld() World { return World{} }\n\n// OnWorld runs on world changes, with a fresh detached snapshot at callback time.\n// Slow handlers may skip intermediate frames. Do not assume one event per packet.\nfunc OnWorld(handler func(World)) Subscription { return Subscription{} }\n\n// Move aims at centered world coordinates, like holding the movement mouse.\n// Refresh at least every 500ms. Returns false while disconnected, without fresh\n// world data, during validation/Init, during manual input (and for 1s afterward),\n// or while another script owns movement. Requests use the normal input cadence.\n// Coordinates outside the playfield are clamped. This is steering, not pathfinding.\nfunc Move(x, y int16) bool { return false }\n\n// MovementState reports a script steering lease and the latest manual movement.\ntype MovementState struct {\n\tActive          bool\n\tOwned           bool // This script owns the active lease.\n\tH, V            int16\n\tExpiresAt       time.Time\n\tLastManualInput time.Time\n}\n\nfunc Movement() MovementState { return MovementState{} }\n\n// StopMoving releases only this script's movement. Reload/stop/logout also release it.\nfunc StopMoving() {}\n\n// Chat trigger kinds\nconst (\n\tChatAny = 1 << iota\n\tChatPlayer\n\tChatNPC\n\tChatCreature\n\tChatSelf\n\tChatOther\n)\n\nconst (\n\tChatTypeSay     = \"say\"\n\tChatTypeYell    = \"yell\"\n\tChatTypeWhisper = \"whisper\"\n\tChatTypeAsk     = \"ask\"\n\tChatTypeExclaim = \"exclaim\"\n\tChatTypeThinkTo = \"think-to\"\n\tChatTypeEmote   = \"emote\"\n)\n\ntype ChatFilter struct {\n\tContains string\n\tSpeaker  string\n\tKinds    int\n\tType     string\n}\n\ntype ChatEvent struct {\n\tSpeaker string\n\tMessage string\n\tRaw     string\n\tKinds   int\n\tType    string\n}\n\nfunc OnChat(filter ChatFilter, handler func(ChatEvent)) Subscription { return Subscription{} }\n\ntype ServerMessageFilter struct {\n\tContains string\n\tType     string\n}\n\ntype ServerMessage struct {\n\tMessage    string\n\tType       string\n\tSequence   uint64\n\tReceivedAt time.Time\n}\n\nfunc OnServerMessage(filter ServerMessageFilter, handler func(ServerMessage)) Subscription {\n\treturn Subscription{}\n}\n\nfunc LatestServerMessage() (ServerMessage, bool) { return ServerMessage{}, false }\n\ntype LifecycleEvent struct {\n\tType              string\n\tCharacter         string\n\tPreviousCharacter string\n\tReason            string\n}\n\nfunc OnLogin(handler func(LifecycleEvent)) Subscription           { return Subscription{} }\nfunc OnLogout(handler func(LifecycleEvent)) Subscription          { return Subscription{} }\nfunc OnCharacterChange(handler func(LifecycleEvent)) Subscription { return Subscription{} }\nfunc OnStop(handler func(LifecycleEvent)) Subscription            { return Subscription{} }\n\nconst (\n\tChangeInventory      = \"inventory\"\n\tChangeEquipment      = \"equipment\"\n\tChangeSelectedPlayer = \"selected-player\"\n\tChangeSelectedItem   = \"selected-item\"\n\tChangeVitals         = \"vitals\"\n\tChangeWorld          = \"world\"\n\tChangeLocation       = \"location\"\n)\n\ntype ChangeEvent struct {\n\tType            string\n\tInventory       []Item\n\tEquipment       []Item\n\tSelectedPlayer  string\n\tSelectedItem    Item\n\tHasSelectedItem bool\n\tHealth          int\n\tHealthMax       int\n\tSpirit          int\n\tSpiritMax       int\n\tBalance         int\n\tBalanceMax      int\n\tLocation        string\n\tWorldGeneration uint64\n}\n\nfunc OnChange(kind string, handler func(ChangeEvent)) Subscription { return Subscription{} }\n\n// Tasks run serially. Wait/WaitTicks/inventory waits inside a task let ordinary\n// callbacks run, so a command or button can cancel it. Cancellation unwinds at\n// the next API call or wait, runs defers, and removes unsent task commands.\n// CPU-only loops still use the normal script execution limits.\ntype Task struct{}\n\nfunc StartTask(fn func()) Task { return Task{} }\nfunc (Task) Cancel()           {}\nfunc (Task) Active() bool      { return false }\n\n// After schedules one callback after successful activation; it is session-only.\nfunc After(delay time.Duration, fn func()) Timer { return Timer{} }\n\n// Time helpers\nfunc WaitTicks(ticks int)         {}\nfunc Wait(duration time.Duration) {}\nfunc WaitForInventory(name string, present bool, timeout time.Duration) bool {\n\treturn false\n}\nfunc WaitForEquipment(name string, equipped bool, timeout time.Duration) bool {\n\treturn false\n}\nfunc Repeat(interval time.Duration, fn func()) Timer { return Timer{} }\n\n// Sound\nfunc PlaySound(ids []uint16) {}\n")},
	{path: "gt2/API_REFERENCE.md", data: []byte("# gt2 Scripting API 2 Reference\n\nGenerated from `pluginapi.go`; do not edit this file directly.\nSource SHA-256: `fce13230beddbec527beca0c01c624e51629f104b5cd4185faa77980c779fdd2`\n\nImport the API with `import \"gt2\"`.\n\n```go\n// Canonical public contract for goThoom scripting API 2.\n// Runtime exports and the generated API reference are verified against this\n// file. Implementations are no-ops so editors can type-check scripts without\n// the full client.\n//\n// Permissions: the client infers requested capabilities from API references.\n// First enable requires user review in a Grant / Block all dialog before globals\n// or Init run. Commands, key bindings, sending server commands, data/events,\n// messages, movement, windows/overlays, input, notifications, storage, Repeat and\n// session events have separate grants. Denied calls do nothing and return zero\n// values or inactive handles. Print, basic configuration, Wait/WaitTicks and\n// cleanup helpers remain available after review. See the script guide for the\n// complete mapping. Decisions are per script ID across characters and persist\n// in Scripts/permissions.json. New capabilities require another review;\n// changing grants restarts the script and releases its existing resources.\npackage gt2\n\nimport \"time\"\n\n// Version and client info\nvar CLVersion int\n\n// Basic output\nfunc Print(msg string)\nfunc ShowNotification(msg string)\n\n// Commands\ntype Subscription struct{}\n\nfunc (Subscription) Remove()\nfunc (Subscription) Active() bool\n\ntype Timer struct{}\n\nfunc (Timer) Stop()\nfunc (Timer) Active() bool\n\nfunc Command(name string, handler func(args string)) Subscription\n\n// QueueCommand returns delivery feedback. Sent means a successful network write,\n// not that the server accepted or completed the action. Cancel only removes unsent work.\nfunc QueueCommand(cmd string) CommandTicket\n\ntype CommandTicket struct{}\n\nfunc (CommandTicket) Status() CommandStatus\nfunc (CommandTicket) Cancel() bool\n\ntype CommandStatus struct{ State, Reason string }\n\nconst (\n\tCommandQueued    = \"queued\"\n\tCommandSent      = \"sent\"\n\tCommandCancelled = \"cancelled\"\n\tCommandRejected  = \"rejected\"\n)\n\n// HasPermission checks a permission ID from the script guide. Unknown IDs return false.\nfunc HasPermission(permission string) bool\n\nfunc Send(cmd string)\n\n// Hotkeys\ntype InputEvent struct {\n\tChord      string\n\tKey        string\n\tButton     string\n\tModifiers  []string\n\tCtrl       bool\n\tAlt        bool\n\tShift      bool\n\tMeta       bool\n\tScreenX    int\n\tScreenY    int\n\tWorldX     int16\n\tWorldY     int16\n\tOnMobile   bool\n\tMobile     Mobile\n\tPlayerName string\n\tSimpleName string\n}\n\nfunc (InputEvent) Consume()\nfunc (InputEvent) Pass()\nfunc (InputEvent) Continues() bool\n\nfunc Bind(combo string, handler func(InputEvent)) Subscription\n\n// Toolbars\ntype ToolbarButton struct {\n\tLabel, Tooltip, Icon, Key string\n\tOnClick                   func()\n}\n\ntype ToolbarOptions struct {\n\tLabel   string\n\tButtons []ToolbarButton\n}\n\n// AddToolbar adds a row to the client toolbar. Icon paths are relative to the\n// root of a folder or ZIP script and are loaded by goThoom.\nfunc AddToolbar(options ToolbarOptions) Subscription\n\n// Script windows are independent, movable client windows. All UI changes are\n// marshaled to the client thread. Button and close callbacks run on the script's\n// serialized event queue. Windows are removed when their script stops/reloads.\ntype WindowButton struct {\n\tID, Label, Tooltip string\n\tIcon               string // Optional bundled Material icon name, such as \"settings\".\n\tDisabled           bool\n\tWidth              int // Optional width in logical pixels for inline rows.\n\tOnClick            func()\n}\n\nconst (\n\tControlText     = \"text\"\n\tControlCheckbox = \"checkbox\"\n\tControlDropdown = \"dropdown\"\n\tControlList     = \"list\"\n\tControlColor    = \"color\"\n\tControlImage    = \"image\"\n)\n\n// WindowControl IDs share a namespace with button IDs. Lists show selectable rows.\n// Color is RGBA packed as 0xRRGGBBAA. OnChange runs for user edits only.\n// Selected is -1 for no selection.\ntype WindowControl struct {\n\tID, Label, Kind, Text, Tooltip string\n\tChecked, Disabled              bool\n\tColor                          uint32\n\tImage                          uint16 // Sprite ID for ControlImage; zero shows an empty preview.\n\tWidth                          int    // Optional width in logical pixels for inline rows.\n\tOptions                        []string\n\tOptionImages                   []uint16 // Optional sprite IDs aligned with list options.\n\tSelected                       int\n\tOnChange                       func(WindowControlEvent)\n}\ntype WindowControlEvent struct {\n\tID, Text string\n\tChecked  bool\n\tColor    uint32\n\tSelected int\n}\n\n// WindowRow arranges controls and buttons on one line. Control labels from the\n// first row form the column headings. IDs are unique across the whole window.\ntype WindowRow struct {\n\tControls []WindowControl\n\tButtons  []WindowButton\n}\n\ntype WindowOptions struct {\n\tControls    []WindowControl\n\tRows        []WindowRow // Editable rows below Controls and above Buttons, with a scrolling viewport.\n\tTitle, Text string\n\tWidth       int            // Content width in logical pixels; default 340, clamped to 220..800.\n\tButtons     []WindowButton // Up to eight buttons, arranged two per row; IDs must be unique.\n\tOnClose     func()         // Called when the user closes the window or Hide is called.\n}\n\ntype Window struct{}\n\n// CreateWindow opens a window after successful script activation. Init and\n// validation stage all UI changes; failed scripts never open a window.\nfunc CreateWindow(options WindowOptions) Window\n\nfunc (Window) SetText(text string)\nfunc (Window) SetButtonEnabled(id string, enabled bool)\n\n// SetRows replaces the inline rows while retaining the window's position.\nfunc (Window) SetRows(rows []WindowRow)\nfunc (Window) SetControlImage(id string, image uint16)\n\n// Programmatic changes do not fire OnChange. Invalid IDs/selections are ignored.\nfunc (Window) SetControlText(id, value string)\nfunc (Window) SetControlChecked(id string, checked bool)\nfunc (Window) SetControlColor(id string, color uint32)\nfunc (Window) SetControlSelected(id string, selected int)\nfunc (Window) SetControlOptions(id string, options []string, optionImages ...[]uint16)\n\nfunc (Window) SetControlEnabled(id string, enabled bool)\nfunc (Window) Show()\nfunc (Window) Hide()\n\n// Remove permanently removes the window without calling OnClose.\nfunc (Window) Remove()\n\n// Active reports whether the window exists, including while hidden.\nfunc (Window) Active() bool\n\n// Shortcuts\nfunc AddShortcut(short, full string)\n\nconst (\n\tScopeGlobal    = \"global\"\n\tScopeCharacter = \"character\"\n)\n\ntype BoolOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 bool\n\tValidate                func(bool) bool\n\tOnChange                func(bool)\n}\n\n// ColorOption stores RGBA as 0xRRGGBBAA and uses the client's color picker.\ntype ColorOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 uint32\n\tValidate                func(uint32) bool\n\tOnChange                func(uint32)\n}\n\ntype IntegerOption struct {\n\tKey, Label, Help, Scope string\n\tDefault, Min, Max, Step int\n\tValidate                func(int) bool\n\tOnChange                func(int)\n}\n\ntype DecimalOption struct {\n\tKey, Label, Help, Scope string\n\tDefault, Min, Max, Step float64\n\tValidate                func(float64) bool\n\tOnChange                func(float64)\n}\n\ntype TextOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tValidate                func(string) bool\n\tOnChange                func(string)\n}\n\ntype ChoiceOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tChoices                 []string\n\tOnChange                func(string)\n}\n\ntype KeyBindingOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tOnChange                func(string)\n}\n\ntype ItemOption struct {\n\tKey, Label, Help, Scope string\n\tDefault                 string\n\tOnChange                func(string)\n}\n\nfunc Bool(option BoolOption) bool\nfunc Color(option ColorOption) uint32\nfunc Integer(option IntegerOption) int\nfunc Decimal(option DecimalOption) float64\nfunc Text(option TextOption) string\nfunc Choice(option ChoiceOption) string\nfunc KeyBinding(option KeyBindingOption) string\nfunc ItemSelector(option ItemOption) string\n\n// OpenSettings opens this script's native preferences, key bindings, and\n// commands page. Calls made before Init completes are ignored.\nfunc OpenSettings()\n\n// Storage is private to the script that calls it, but shared by every\n// character using that script. Include the current character in the key when\n// data should be character-specific.\nfunc Store(key string, value any)\nfunc LoadString(key, fallback string) string\nfunc LoadBool(key string, fallback bool) bool\nfunc LoadInteger(key string, fallback int) int\nfunc LoadDecimal(key string, fallback float64) float64\nfunc LoadStrings(key string, fallback []string) []string\nfunc LoadJSON(key string, target any) bool\nfunc DeleteStored(key string)\nfunc MigrateStorage(version int, migrate func(fromVersion int))\n\n// Storage is a character-bound handle. An unknown character produces an inactive\n// handle; obtain a new one after login. Retained handles can save during logout\n// and Terminate. Methods preserve ordinary storage fallbacks.\ntype Storage struct{}\n\nfunc CharacterStore() Storage\nfunc (Storage) Active() bool\nfunc (Storage) Store(key string, value any)\nfunc (Storage) LoadString(key, fallback string) string\nfunc (Storage) LoadBool(key string, fallback bool) bool\nfunc (Storage) LoadInteger(key string, fallback int) int\nfunc (Storage) LoadDecimal(key string, fallback float64) float64\nfunc (Storage) LoadStrings(key string, fallback []string) []string\nfunc (Storage) LoadJSON(key string, target any) bool\nfunc (Storage) DeleteStored(key string)\n\n// Input box helpers\nfunc InputText() string\nfunc SetInputText(text string)\n\n// Player and world info\ntype Player struct {\n\tName         string\n\tRace         string\n\tGender       string\n\tClass        string\n\tPictID       uint16\n\tColors       []byte\n\tIsNPC        bool\n\tSharee       bool\n\tSharing      bool\n\tFriend       bool\n\tFriendLabel  int\n\tLocalLabel   int\n\tGlobalLabel  int\n\tBlocked      bool\n\tIgnored      bool\n\tDead         bool\n\tFellWhere    string\n\tFellTime     time.Time\n\tKillerName   string\n\tBard         bool\n\tSameClan     bool\n\tSeen         bool\n\tLastSeen     time.Time\n\tLastOnScreen time.Time\n\tOffline      bool\n}\n\ntype Character struct {\n\tName       string\n\tHealth     int\n\tHealthMax  int\n\tSpirit     int\n\tSpiritMax  int\n\tBalance    int\n\tBalanceMax int\n\tLocation   string\n\tEquipment  []Item\n}\n\nfunc Self() Character\nfunc Players() []Player\n\n// PlayerChangeEvent reports observed changes, with detached before/after snapshots.\n// First observation establishes a baseline. New known players use PlayerDiscovered;\n// login/logout mean an observed Offline transition, not entering/leaving the screen.\ntype PlayerChangeEvent struct {\n\tType             string\n\tPrevious, Player Player\n}\n\nconst (\n\tPlayerDiscovered = \"discovered\"\n\tPlayerRemoved    = \"removed\"\n\tPlayerLogin      = \"login\"\n\tPlayerLogout     = \"logout\"\n\tPlayerFallen     = \"fallen\"\n\tPlayerRecovered  = \"recovered\"\n\tPlayerSharing    = \"sharing\"\n)\n\nfunc OnPlayerChange(handler func(PlayerChangeEvent)) Subscription\n\n// Inventory\ntype Item struct {\n\tInstanceID uint64\n\tID         uint16\n\tName       string\n\tBase       string\n\tExtra      string\n\tEquipped   bool\n\tIndex      int\n\tIDIndex    int\n\tQuantity   int\n\tSlot       string\n}\n\nconst (\n\tSlotForehead  = \"forehead\"\n\tSlotNeck      = \"neck\"\n\tSlotShoulder  = \"shoulder\"\n\tSlotArms      = \"arms\"\n\tSlotGloves    = \"gloves\"\n\tSlotFinger    = \"finger\"\n\tSlotCoat      = \"coat\"\n\tSlotCloak     = \"cloak\"\n\tSlotTorso     = \"torso\"\n\tSlotWaist     = \"waist\"\n\tSlotLegs      = \"legs\"\n\tSlotFeet      = \"feet\"\n\tSlotRightHand = \"right-hand\"\n\tSlotLeftHand  = \"left-hand\"\n\tSlotBothHands = \"both-hands\"\n\tSlotHead      = \"head\"\n)\n\nfunc Inventory() []Item\nfunc EquippedItems() []Item\nfunc FindItemExact(name string) (Item, bool)\nfunc FindItem(name string) (Item, bool)\nfunc FindItems(name string) []Item\nfunc SearchItems(text string) []Item\nfunc Equipped(slot string) (Item, bool)\nfunc HasItem(name string) bool\nfunc IsEquipped(name string) bool\n\n// Equip equips an item by name (case-insensitive).\nfunc Equip(name string)\n\n// Unequip unequips an item by name (case-insensitive).\nfunc Unequip(name string)\n\nfunc WithEquipment(name string, task func())\n\n// Images and world overlay\nfunc WorldSize() (int, int)\nfunc ImageSize(id uint16) (int, int)\nfunc OverlayClear()\nfunc OverlayRect(x, y, w, h int, r, g, b, a uint8)\nfunc OverlayCircle(x, y, radius int, r, g, b, a uint8)\nfunc OverlayText(x, y int, txt string, r, g, b, a uint8)\nfunc OverlayImage(id uint16, x, y int)\nfunc OverlayFollowPlayer(name string, x, y, radius int, r, g, b, a uint8, maxLife time.Duration)\nfunc OverlayFollowMobile(index uint8, x, y, radius int, r, g, b, a uint8, maxLife time.Duration)\nfunc OverlayFollowBackground(pictID uint16, x, y, radius int, r, g, b, a uint8, maxLife time.Duration)\n\n// SetMobileTint applies a multiplicative RGBA tint to every visible mobile\n// using id. The tint belongs to the calling script and is released when it\n// stops or reloads. More than one script tinting an ID uses the\n// lexicographically last script ID.\nfunc SetMobileTint(id uint16, r, g, b, a uint8)\n\n// ClearMobileTint removes this script's tint for one mobile sprite ID.\nfunc ClearMobileTint(id uint16)\n\n// ClearMobileTints removes every mobile tint owned by this script.\nfunc ClearMobileTints()\n\n// SetMobileOutline draws a sprite-shaped outline around every visible mobile\n// using id. It follows the client's interpolated mobile position and is\n// released when the calling script stops or reloads.\nfunc SetMobileOutline(id uint16, r, g, b, a uint8)\n\n// ClearMobileOutline removes this script's outline for one mobile sprite ID.\nfunc ClearMobileOutline(id uint16)\n\n// ClearMobileOutlines removes every mobile outline owned by this script.\nfunc ClearMobileOutlines()\n\n// SetNamedMobileTint applies a tint to an exact mobile name, ignoring case and\n// leading/trailing spaces. It matches independently of sprite ID and takes\n// precedence over sprite-ID tints. Conflicting name tints use the\n// lexicographically last script ID. Empty names are ignored. Stop/reload and\n// ClearMobileTints release these tints along with sprite-ID tints.\nfunc SetNamedMobileTint(name string, r, g, b, a uint8)\n\n// ClearNamedMobileTint removes this script's tint for one mobile name.\nfunc ClearNamedMobileTint(name string)\n\n// SetNamedMobileOutline draws an outline for an exact mobile name, using the\n// same matching and precedence rules as SetNamedMobileTint. Stop/reload and\n// ClearMobileOutlines release both named and sprite-ID outlines.\nfunc SetNamedMobileOutline(name string, r, g, b, a uint8)\n\n// ClearNamedMobileOutline removes this script's outline for one mobile name.\nfunc ClearNamedMobileOutline(name string)\n\n// FlashMobile replaces one visible mobile's colors for duration, preserving its\n// sprite transparency. Alpha is the flash strength: 255 is solid color, and\n// lower values mix with the normal artwork. The flash belongs to\n// the calling script and is released when it stops or reloads. More than one\n// script flashing an index uses the lexicographically last script ID.\nfunc FlashMobile(index uint8, r, g, b, a uint8, duration time.Duration)\n\n// Mobile coordinates are unscaled, relative to the center of the playfield.\n// Index is a temporary descriptor slot; use Name to track a player across scenes.\ntype Mobile struct {\n\tIndex  uint8\n\tName   string\n\tH, V   int16\n\tPictID uint16\n\tColors uint8\n\tPlayer bool\n\tState  uint8 // Raw protocol animation state.\n\tPlane  int   // Draw order, not a collision layer.\n\tSize   int   // Sprite frame size, not a collision radius.\n\tSelf   bool\n\tDead   bool\n\tStale  bool // Retained for edge rendering; absent from the current server frame.\n}\n\ntype Click struct {\n\tX, Y     int16\n\tOnMobile bool\n\tOnPlayer bool\n\tMobile   Mobile\n\tButton   string\n\tCtrl     bool\n\tAlt      bool\n\tShift    bool\n\tMeta     bool\n}\n\nfunc LastClick() Click\nfunc Hover() Click\n\nfunc SelectedPlayer() (Player, bool)\nfunc SelectedItem() (Item, bool)\n\n// Picture describes scenery artwork. H,V is its top-left corner in centered\n// world coordinates. Dimensions describe one animation frame. No collision\n// geometry is sent by the server; planes and artwork are only navigation hints.\ntype Picture struct {\n\tPictID             uint16\n\tH, V               int16\n\tWidth, Height      int\n\tPlane              int\n\tMoving, Background bool\n\tReused             bool // Reused from a preceding frame, including retained edge artwork.\n\tShadow             bool\n}\n\ntype World struct {\n\tWidth                      int\n\tHeight                     int\n\tLocation                   string\n\tGeneration                 uint64\n\tMobiles                    []Mobile // Includes fallen and retained edge mobiles; check Dead/Stale.\n\tPictures                   []Picture\n\tSelf                       Mobile\n\tHasSelf                    bool\n\tFrame                      int // Server logical frame; may reset on reconnect or movie seek.\n\tReceivedAt                 time.Time\n\tCameraShiftX, CameraShiftY int   // Estimated scenery shift since the preceding frame.\n\tLighting                   uint8 // Raw protocol lighting flags.\n}\n\nfunc CurrentWorld() World\n\n// OnWorld runs on world changes, with a fresh detached snapshot at callback time.\n// Slow handlers may skip intermediate frames. Do not assume one event per packet.\nfunc OnWorld(handler func(World)) Subscription\n\n// Move aims at centered world coordinates, like holding the movement mouse.\n// Refresh at least every 500ms. Returns false while disconnected, without fresh\n// world data, during validation/Init, during manual input (and for 1s afterward),\n// or while another script owns movement. Requests use the normal input cadence.\n// Coordinates outside the playfield are clamped. This is steering, not pathfinding.\nfunc Move(x, y int16) bool\n\n// MovementState reports a script steering lease and the latest manual movement.\ntype MovementState struct {\n\tActive          bool\n\tOwned           bool // This script owns the active lease.\n\tH, V            int16\n\tExpiresAt       time.Time\n\tLastManualInput time.Time\n}\n\nfunc Movement() MovementState\n\n// StopMoving releases only this script's movement. Reload/stop/logout also release it.\nfunc StopMoving()\n\n// Chat trigger kinds\nconst (\n\tChatAny = 1 << iota\n\tChatPlayer\n\tChatNPC\n\tChatCreature\n\tChatSelf\n\tChatOther\n)\n\nconst (\n\tChatTypeSay     = \"say\"\n\tChatTypeYell    = \"yell\"\n\tChatTypeWhisper = \"whisper\"\n\tChatTypeAsk     = \"ask\"\n\tChatTypeExclaim = \"exclaim\"\n\tChatTypeThinkTo = \"think-to\"\n\tChatTypeEmote   = \"emote\"\n)\n\ntype ChatFilter struct {\n\tContains string\n\tSpeaker  string\n\tKinds    int\n\tType     string\n}\n\ntype ChatEvent struct {\n\tSpeaker string\n\tMessage string\n\tRaw     string\n\tKinds   int\n\tType    string\n}\n\nfunc OnChat(filter ChatFilter, handler func(ChatEvent)) Subscription\n\ntype ServerMessageFilter struct {\n\tContains string\n\tType     string\n}\n\ntype ServerMessage struct {\n\tMessage    string\n\tType       string\n\tSequence   uint64\n\tReceivedAt time.Time\n}\n\nfunc OnServerMessage(filter ServerMessageFilter, handler func(ServerMessage)) Subscription\n\nfunc LatestServerMessage() (ServerMessage, bool)\n\ntype LifecycleEvent struct {\n\tType              string\n\tCharacter         string\n\tPreviousCharacter string\n\tReason            string\n}\n\nfunc OnLogin(handler func(LifecycleEvent)) Subscription\nfunc OnLogout(handler func(LifecycleEvent)) Subscription\nfunc OnCharacterChange(handler func(LifecycleEvent)) Subscription\nfunc OnStop(handler func(LifecycleEvent)) Subscription\n\nconst (\n\tChangeInventory      = \"inventory\"\n\tChangeEquipment      = \"equipment\"\n\tChangeSelectedPlayer = \"selected-player\"\n\tChangeSelectedItem   = \"selected-item\"\n\tChangeVitals         = \"vitals\"\n\tChangeWorld          = \"world\"\n\tChangeLocation       = \"location\"\n)\n\ntype ChangeEvent struct {\n\tType            string\n\tInventory       []Item\n\tEquipment       []Item\n\tSelectedPlayer  string\n\tSelectedItem    Item\n\tHasSelectedItem bool\n\tHealth          int\n\tHealthMax       int\n\tSpirit          int\n\tSpiritMax       int\n\tBalance         int\n\tBalanceMax      int\n\tLocation        string\n\tWorldGeneration uint64\n}\n\nfunc OnChange(kind string, handler func(ChangeEvent)) Subscription\n\n// Tasks run serially. Wait/WaitTicks/inventory waits inside a task let ordinary\n// callbacks run, so a command or button can cancel it. Cancellation unwinds at\n// the next API call or wait, runs defers, and removes unsent task commands.\n// CPU-only loops still use the normal script execution limits.\ntype Task struct{}\n\nfunc StartTask(fn func()) Task\nfunc (Task) Cancel()\nfunc (Task) Active() bool\n\n// After schedules one callback after successful activation; it is session-only.\nfunc After(delay time.Duration, fn func()) Timer\n\n// Time helpers\nfunc WaitTicks(ticks int)\nfunc Wait(duration time.Duration)\nfunc WaitForInventory(name string, present bool, timeout time.Duration) bool\n\nfunc WaitForEquipment(name string, equipped bool, timeout time.Duration) bool\n\nfunc Repeat(interval time.Duration, fn func()) Timer\n\n// Sound\nfunc PlaySound(ids []uint16)\n```\n")},
}
